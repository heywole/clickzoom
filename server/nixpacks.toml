[phases.setup]
nixPkgs = ["ffmpeg", "chromium", "nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm install --prefix ../client", "npm run build --prefix ../client"]

[start]
cmd = "node index.js"
